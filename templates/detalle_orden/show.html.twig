{% extends 'base.html.twig' %}

{% block title %}DetalleOrden{% endblock %}

{% block body %}
    <h1>DetalleOrden</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detalle_orden.id }}</td>
            </tr>
            <tr>
                <th>FechaIngreso</th>
                <td>{{ detalle_orden.fechaIngreso ? detalle_orden.fechaIngreso|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>FechaEntrega</th>
                <td>{{ detalle_orden.fechaEntrega ? detalle_orden.fechaEntrega|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Observacion</th>
                <td>{{ detalle_orden.observacion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('detalle_orden_index') }}">back to list</a>

    <a href="{{ path('detalle_orden_edit', {'id': detalle_orden.id}) }}">edit</a>

    {{ include('detalle_orden/_delete_form.html.twig') }}
{% endblock %}
