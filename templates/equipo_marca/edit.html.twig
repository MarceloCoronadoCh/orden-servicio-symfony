{% extends 'base.html.twig' %}

{% block title %}Edit EquipoMarca{% endblock %}

{% block body %}
    <h1>Editar Marca</h1>

    {{ include('equipo_marca/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('equipo_marca_index') }}">back to list</a>

    {{ include('equipo_marca/_delete_form.html.twig') }}
{% endblock %}
