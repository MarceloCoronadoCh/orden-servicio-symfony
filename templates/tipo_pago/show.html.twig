{% extends 'base.html.twig' %}

{% block title %}TipoPago{% endblock %}

{% block body %}
    <h1>TipoPago</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_pago.id }}</td>
            </tr>
            <tr>
                <th>NombreTipoPago</th>
                <td>{{ tipo_pago.nombreTipoPago }}</td>
            </tr>
            <tr>
                <th>DescripcionTipoPago</th>
                <td>{{ tipo_pago.descripcionTipoPago }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipo_pago_index') }}">back to list</a>

    <a href="{{ path('tipo_pago_edit', {'id': tipo_pago.id}) }}">edit</a>

    {{ include('tipo_pago/_delete_form.html.twig') }}
{% endblock %}
