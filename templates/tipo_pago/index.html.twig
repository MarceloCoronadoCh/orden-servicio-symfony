{% extends 'base.html.twig' %}

{% block title %}TipoPago index{% endblock %}

{% block body %}
    <h1>TipoPago index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NombreTipoPago</th>
                <th>DescripcionTipoPago</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tipo_pago in tipo_pagos %}
            <tr>
                <td>{{ tipo_pago.id }}</td>
                <td>{{ tipo_pago.nombreTipoPago }}</td>
                <td>{{ tipo_pago.descripcionTipoPago }}</td>
                <td>
                    <a href="{{ path('tipo_pago_show', {'id': tipo_pago.id}) }}">show</a>
                    <a href="{{ path('tipo_pago_edit', {'id': tipo_pago.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('tipo_pago_new') }}">Create new</a>
{% endblock %}
